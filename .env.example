# Aurora OS Feature Flags

# Export Guard Policy: off|warn|strict (default: warn)
# off: No validation, warnings, or manifest requirements
# warn: Validation with warnings, auto-attach missing manifests
# strict: Require explicit manifests and fail on validation errors
AURORA_EXPORT_POLICY=warn

# Glyphcard Generation: 0|1 (default: 0)
# Enable automatic glyphcard generation for exports
AURORA_GLYPHCARDS=0

# Glyphcard Redaction: 0|1 (default: 1)
# When enabled, glyphcards exclude sensitive payload excerpts
AURORA_GLYPHCARD_REDACT=1